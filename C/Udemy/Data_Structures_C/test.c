#include<stdio.h>
#include<string.h>
#include<stdlib.h>

void main(){
    // int n;
    // scanf("%d",&n);
    // int a[n];
    // // a[0] = 0;
    // // a[1] = 1;
    // // a[2] = 2;
    // int *p;
    // p = a;
    // p = (int *)realloc(p, 10*sizeof(int));
    printf("%d\n",__INT16_MAX__);
}


